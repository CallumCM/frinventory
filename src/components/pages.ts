import { html } from 'hono/html'

// Page components are just functions that return html`` tagged templates
// They can be composed together and passed as children to Layout
export const HomePage = () => html``
